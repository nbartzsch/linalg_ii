\documentclass[12pt, a4paper]{article}
\usepackage{script}
\usepackage{gauss}
\usepackage[ngerman]{babel}

\usetikzlibrary{external} % for faster compilation externalization of the tikz figures is used
\tikzexternalize[prefix=tikz/] % when active use this to compile in terminal: pdflatex -shell-escape theory_sheet.tex (not needed when using build.sh)

\setlength{\headheight}{14.49998pt}
\addtolength{\topmargin}{-2.49998pt}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{Lineare Algebra II FS25} 
\fancyhead[R]{Nicolas Bartzsch}
\fancyfoot[C]{\thepage}

\setboolean{includingsolutions}{false} % true: includes solutions, % false, no solutions (not used here) 

% Dynamically include only the desired chapter
\providecommand{\selectedchapter}{}
\includeonly{
    chapters/\selectedchapter
}

% Custom matrices for the guass.sty package. Has to be the same as in main.tex
\newmatrix{(}{|}{L}
\newmatrix{.}{)}{R}
\newmatrix{(}{.}{m}

\begin{document}

    \pagenumbering{arabic}
    
    \include{chapters/lineare_abb}
    \include{chapters/eigenwerte}
    \include{chapters/normen_skalarprod}
    \include{chapters/orthonormalbasis}
    \include{chapters/kern_bild}
    \include{chapters/basiswechsel_diag}
    \include{chapters/quadratische_formen}
    \include{chapters/hauptachsentrans}
    \include{chapters/kleinste_quadrate_qr}
    \include{chapters/lin_diff_sys}

\end{document}
