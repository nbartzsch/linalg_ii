\documentclass[12pt, a4paper]{article}
\usepackage{script}
\usepackage{gauss}
\usepackage[ngerman]{babel}

% section for external compiling when using pgfplots. If not needed, comment out
% when active use this to compile in terminal: pdflatex -shell-escape theory_sheet.tex
% when changing chapters first delete exsisting tikz files in tikz/ folder

% Ensure TikZ externalization works with -shell-escape
\usetikzlibrary{external}
\tikzexternalize[prefix=tikz/]
% \tikzexternalize[prefix=tikz/, mode=only graphics] % Use 'only graphics' mode for externalization

% Instructions for compiling with -shell-escape
% To compile with externalized TikZ graphics, use:
% pdflatex -shell-escape "\def\chaptername{<chapter_name>}\input{theory_sheet.tex}"

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\setlength{\headheight}{14.49998pt}
\addtolength{\topmargin}{-2.49998pt}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{Lineare Algebra II FS25} 
\fancyhead[R]{Nicolas Bartzsch}
\fancyfoot[C]{\thepage}

\setboolean{includingsolutions}{false} % true: includes solutions, % false, no solutions

% Dynamically include only the desired chapter
\providecommand{\selectedchapter}{}
\includeonly{
    chapters/\selectedchapter
}

\begin{document}

    % Prompt for chapter selection
    % To compile a specific chapter, set the chapter name using:
    % pdflatex -shell-escape '\def\selectedchapter{<chapter_name>}\input{theory_sheet.tex}'

    %Inhalt
    \pagenumbering{arabic}
    
    \include{chapters/lineare_abb}
    \include{chapters/eigenwerte}
    \include{chapters/normen_skalarprod}
    \include{chapters/orthonormalbasis}
    \include{chapters/kern_bild}
    \include{chapters/basiswechsel_diag}
    \include{chapters/quadratische_formen}
    \include{chapters/hauptachsentrans}
    \include{chapters/kleinste_quadrate_qr}
    \include{chapters/lin_diff_sys}

\end{document}
